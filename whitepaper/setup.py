from setuptools import setup

setup(
    name='whitepaper',
    author='Sabine Bertram',
    install_requires=['mechanicalsoup', 'requests', 'tika', 'nltk', 'gensim', 'pytest', 'mock'],
    version='0.1',
)

